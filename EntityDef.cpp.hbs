{{#each pairs}}
#include "{{this.Item1}}.h"
{{/each}}

using namespace ResWolf;

Entity* ResWolf::createEntity(int id, uint16_t x, uint16_t y, char* config)
{
    switch(id)
    {
    {{#each pairs}}
    case {{Up this.Item1}}:
    {
        return new {{this.Item1}}(id, x, y, config);
    }
    {{/each}}
    }
}