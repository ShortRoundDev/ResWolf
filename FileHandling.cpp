#include "FileHandling.h"

#include <cstdlib>
#include <cstdio>
#include <string>

int ReadTextFile(std::string path, char* file)
{
	/*int status = 0;
	FILE* fp = fopen(path.c_str(), "r");
	if (!fp)
	{
		status |= FILE_FAILED;
		
	}*/
}

int ReadBinaryFile(LPWSTR path, char* file)
{

}